{
    "version": 2,
    "builds": [
        {
            "src": "api/index.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/index"
        }
    ],
    "env": {
        "PRISMA_CLI_BINARY_TARGETS": "native,rhel-openssl-1.0.x"
    }
}
